version: 2.1
jobs:
  terraform_apply:
    working_directory: ~/tmp
    docker:
            - image: hashicorp/terraform:full
    steps:
      - checkout
      - run:
          name: terraform apply
          command: |
            terraform init

workflows:
  version: 2.1
  terraform:
    jobs:
      - terraform_apply

